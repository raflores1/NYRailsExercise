<%= form_for @photopost  do |f| %>
    <% if @photopost.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@photopost.errors.count, "error") %> prohibited this contact from being saved:</h2>

          <ul>
            <% @photopost.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </div>
    <div class="field">
      <%= f.label :url %><br>
      <%= f.url_field :url %>
    </div>
    <button class = 'btn btn-primary'><%f.submit %>Submit</button>

<% end %>